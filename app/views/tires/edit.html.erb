<h1><%= @tire.tirename %> の編集ページ</h1>

<%= form_for(@tire) do |f| %>
  <%= f.label :tirename, '車種名' %>
  <%= f.text_field :tirename %>

  <%= f.submit '投稿' %>
<% end %>

<%= link_to '一覧に戻る', tires_path %>
<%= link_to 'この車種を削除する', @tire, method: :delete, data: { confirm: '本当に削除してよろしいですか？' } %>
