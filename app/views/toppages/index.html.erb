<% if logged_in? %>
  <%= render 'users/userdata' %>
<% else %>
  <div class="btn-wrap center">
      <div class="btn"><%= link_to 'signup', signup_path %></div>
      <div class="btn"><%= link_to 'login', login_path %></div>
  </div>
<% end %>
